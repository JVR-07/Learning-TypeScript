# Plan de Aprendizaje: TypeScript (Transición desde C#)

## 🟢 Nivel 1: Fundamentos y DOM (✅ Completado)

**Objetivo:** Comprender la configuración del entorno y la manipulación básica de tipos en el navegador.

**Conceptos Clave:**

- Inferencia de tipos vs. Tipado explícito.

- Casting de tipos en el DOM (`as HTMLInputElement`).

- Manejo de eventos (`Event Listeners`).

- Configuración de `tsconfig.json` y compilación.

## 🟡 Nivel 2: Estructuras de Datos y Modelado

**Objetivo:** Comprender el "Tipado Estructural" y la modularidad, diferenciándolo del sistema nominal de C#.

**Conceptos a Desarrollar:**

- **Interfaces:** Definir la forma de los datos (`Duck Typing`).

- **Tipos Literales y Union Types:** Definición de valores específicos (ej. `status: 'pending' | 'completed'`).

- **Manejo de Arrays:** Manipulación inmutable (`map, filter`) vs. bucles imperativos.

- **Módulos (ES Modules):** Importación y exportación para separar lógica y vista.

## 🔴 Nivel 3: Tipado Avanzado y Asincronía

**Objetivo:** Crear código reutilizable y robusto utilizando características avanzadas del lenguaje.

**Conceptos a Desarrollar:**

- **Genéricos (`<T>`):** Funciones y componentes flexibles.

- **Async/Await:** Manejo de Promesas y flujo asíncrono.

- **Utility Types:** Herramientas nativas como `Partial<T>, Pick<T>, Omit<T>`.

- **Type Narrowing:** Refinamiento de tipos en tiempo de ejecución.
